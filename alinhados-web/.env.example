# Supabase (auth) — decisão verbal da daily 03/07 (ADR-0001 formal pendente).
# Sem estas variáveis o app usa o MockAuthGateway (localStorage, só para dev).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# alinhados-api local (docker compose up no alinhados-api)
NEXT_PUBLIC_API_URL=http://localhost:3000
